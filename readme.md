To run the server:

1. `npm install` to install of the modules
2. To set up the SQL server run the `setup.sql` script
3. `node app.js` and the server will be listening on port 9001

Available routes can be found in the `APIDocumentation.md` file.